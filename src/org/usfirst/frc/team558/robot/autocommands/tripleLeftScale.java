package org.usfirst.frc.team558.robot.autocommands;

import org.usfirst.frc.team558.robot.auto_subsystems.*;
import org.usfirst.frc.team558.robot.paths.*;
import edu.wpi.first.wpilibj.command.CommandGroup;

/**
 *
 */
public class tripleLeftScale extends CommandGroup {

    public tripleLeftScale() {
	    	/*addSequential(new Arm(0, .25));
	    	addParallel(new PunchWithSensors(5));
	    	addSequential(new FollowArc(new tripleScaleArc()));
	    	addSequential(new zeroPidgeotto());
	    	addParallel(new Arm(-10600, .25));
	    	addParallel(new rollerClawSensor(-1, 4));
	    	addSequential(new FollowArc(new tripleScaleFrontArc()));
	    	addSequential(new zeroPidgeotto());
	    	addSequential(new Arm(0, .25));
	    	addParallel(new PunchWithSensors(5));
	    	addSequential(new FollowArc(new tripleScaleBack2Arc()));
	    	addSequential(new zeroPidgeotto());
	    	//addSequential(new FollowArc(new tripleScaleFront2Arc()));
	    //addSequential(new zeroPidgeotto());
	    	//addSequential(new FollowArc(new tripleScaleBack3Arc()));*/
    }
}
